Orders : 
    {
        ID: integer not null,
        user : user ID,
        price : float,
    }

    Address : {
        street : street name,
        postCode : postal code,
        number : street number,
        user : userID
    }

    Users :{
        ID : integer not null,
        name : user name,
        email : user email,
        address : address ID
    }

    OrderedProducts : {
        ID : integer not null,
        productID : productID,
        quantity : quantity ordered
    }

    Products :{
        ID : product ID,
        name : product name, 
        price : product price
    }

    create table orderedProducts (orderID integer not null,productID integer not null, quantity integer default 1, primary key(orderID,productID), foreign key (orderId) references orders(ID), foreign key (productID) references products(ID);

    ALTER TABLE orderedProducts ADD FOREIGN KEY (orderId) REFERENCES orders(ID);

    CREATE TABLE Address
create table orderAddress ( ID integer not null auto_increment primary key, streetName varchar(100), postcode varchar(100), city varchar (100), prov varchar(100), name varchar(100), unique key (streetName, postcode));


create table orders ( ID integer not null auto_increment primary key, user integer not null, total float not null)
